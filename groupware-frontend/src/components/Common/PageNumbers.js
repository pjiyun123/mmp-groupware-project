import React from 'react';

const PageNumbers = () => {
	return (
		<div className='pageNumbersContainer'>
			<button>{'<'}</button>
			<button>{'>'}</button>
		</div>
	);
};

export default PageNumbers;